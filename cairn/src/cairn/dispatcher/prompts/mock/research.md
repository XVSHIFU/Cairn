# 自主安全研究任务（mock 测试用）

目标：{objective}
方向：{next_direction}
预算：{budget_json}
已用：{usage_json}
授权：{authorization_json}
已有证据：{evidence_summary}

输出 JSON，字段：summary, evidence(list), findings(list), next_direction, phase, terminal, awaiting_input。